import React from "react"
import PropTypes from "prop-types"

const CartScreen = (props) => {
  return (
    <div>
      <h2>Cart is on building...</h2>
    </div>
  )
}

CartScreen.propTypes = {}

export default CartScreen
